"""Setup file for emtools package."""

from setuptools import setup

setup(
    name='emtools',
    version='0.1.0',
    author='Andrew A. Herzing',
    description='Python tools for processing electron microscopy data ',
    packages=[
        'emtools',
        ],
    install_requires=[
    ]
)
